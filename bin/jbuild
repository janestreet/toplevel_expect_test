(executables
 ((names (main))
  (link_flags (-linkall))
  (libraries (toplevel_expect_test))))

(provides toplevel_expect_test:main.cmo)

(install
 ((section bin)
  (files ((main.bc as ocaml-expect)))))


(jbuild_version jane_street)
